[project]
name = "finexpert"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mani1234765", email = "manideepdasanapu2003@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.13.5",
    "scikit-learn>=1.9.1",
]

[project.scripts]
finexpert = "finexpert:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
